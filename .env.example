# ── Razorpay (get from https://dashboard.razorpay.com > Settings > API Keys) ──
# Use TEST keys during development, LIVE keys in production

RAZORPAY_KEY_ID=rzp_test_SY5aF2TTvUKokb
RAZORPAY_KEY_SECRET=QpcirhBvErcQrFJvRlkgCcm6

# ── Frontend (Vite exposes VITE_ prefixed vars to the browser) ─────────────
# This is your KEY ID only — never expose the secret to the frontend
VITE_RAZORPAY_KEY_ID=rzp_test_SY5aF2TTvUKokb

# ── Admin auth (JWT + secure cookie session) ───────────────────────────────
# Demo credentials for the login page. Replace these before any real use.
AUTH_ADMIN_EMAIL=admin@booknest.local
AUTH_ADMIN_PASSWORD=BookNest@2026
AUTH_USER_EMAIL=reader@booknest.local
AUTH_USER_PASSWORD=Reader@2026

# Long, random secret used to sign JWTs on the server.
JWT_SECRET=replace-this-with-a-long-random-string
